# JUROR backend — required Python packages.
#
# Tested with Python 3.10+. All packages are pinned to compatible majors so
# `pip install -r requirements.txt` reproduces the same environment.

# Web framework
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0

# HTTP client used to call LLM providers
httpx>=0.26,<1.0

# Triage stage: classical ML
scikit-learn>=1.3,<2.0
scipy>=1.10,<2.0
numpy>=1.24,<2.0
pandas>=2.0,<3.0

# Fact-Checker stage: real NLI model
transformers>=4.40,<5.0
torch>=2.1,<3.0
sentencepiece>=0.2,<1.0       # required by some NLI tokenizers
